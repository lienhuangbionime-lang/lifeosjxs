# Universal AI Bootstrapper Rules

> **[CRITICAL DIRECTIVE FOR ALL AI AGENTS]**
> This project is under the command of Lien Huang (Commander).
> You are inheriting a highly evolved Meta-Development Architecture. 
> 
> **FIRST ACTION MANDATE:**
> Before you write ANY code, analyze ANY files, or make ANY architectural decisions, you must ALWAYS execute the following command to download the Commander's latest rules from the Supabase Cloud Memory:
> 
> ```bash
> python tools/sync_dev_rules.py
> ```
> 
> If `tools/sync_dev_rules.py` does not exist yet, ask the Commander to provide the AI Bootstrapper Kit.
> 
> By running this script, you will automatically inject the `SYSTEM_CONTEXT.md` and the full `.cursorrules` specific to the Commander's latest coding style (e.g., handling Schema Drift, safe_write, Vanilla CSS, strict async, etc.).
> 
> Do not rely solely on your baseline training. Connect to the Cloud Memory now.
